import React from "react";
import Footer from "./Footer";

const Footerbar = ({ themeColor, setThemeColor }) => (
  <Footer themeColor={themeColor} setThemeColor={setThemeColor} />
);

export default Footerbar;
